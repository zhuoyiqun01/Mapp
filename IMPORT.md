# 数据迁出格式规范

本文档面向**其他节点—连线可视化软件的开发者**：若希望用户从贵方软件导出数据后，能在本网站（Bibliometrics）直接导入使用，请按下列格式约定输出文件。

**原则**

- 仅接受 **UTF-8 纯文本**（`.txt`、`.csv`、`.tsv`、`.net`、`.clu`、`.vec`、`.map`、`.json`）。
- 不接受 Excel、PDF、二进制专有格式。
- 节点与连线可合并为单文件，也可拆成多个文件；**补充属性**（聚类、额外变量）可通过独立文件按 `id` 合并。

---

## 1. 本网站所需的数据语义

无论来源软件如何实现，导入后需能映射为以下逻辑字段：

| 逻辑字段 | 必填 | 说明 |
|----------|------|------|
| `id` | **是** | 节点唯一标识，字符串；连线端点必须能对应到已有节点的 `id` |
| `label` | 推荐 | 主显示名 |
| `label2` | 否 | 副标题（如年份），与 `label` 组合显示 |
| `x`, `y` | 否 | 布局坐标，数值 |
| 其他数值列 | 否 | 作为 **Variable**（如频次、中心性），供大小/颜色/坐标轴使用 |
| 文本列 | 否 | 作为 **text**（如标题、来源），显示在节点详情 |
| `cluster` | 否 | 聚类编号（整数或字符串） |
| `clusterName` | 否 | 聚类名称 |
| `source`, `target` | 有连线时必填 | 边的两端 `id` |
| `weight` | 否 | 边权重，默认 `1` |

**合并键**：所有补充文件均以 `id` 与主节点表对齐；`id` 不一致的行会被忽略。

---

## 2. 支持的文件类型一览

| 类型 | 典型扩展名 | 用途 |
|------|------------|------|
| 节点表 | `.txt` `.csv` `.tsv` `.map` | 主节点列表 |
| 连线表 | `.txt` `.csv` | 边列表（需先有节点） |
| 节点+连线合并 | `.txt` `.net` | VOSviewer 式合并导出，或 Pajek |
| Pajek 网络 | `.net` | `*Vertices` + 可选 `*Edges` |
| Pajek 聚类 | `.clu` | 按顶点序号补充 `cluster` |
| Pajek 向量 | `.vec` | 按顶点序号补充标量变量 |
| CiteSpace 可视化 JSON | `.json` | **仅补充**；须含 `nodes[]`；**不能**单独成图（见 §7） |
| 补充预设包 | `.txt` | 本网站导出的带列映射的数据包（可互操作） |

一次可上传多个文件；网站按 **节点 → 连线 → 补充** 顺序自动识别与合并（最多同时处理 4 个文件）。

---

## 3. 节点表（通用表格式）

### 3.1 文件结构

- 第一行为**表头**，第二行起为数据。
- 分隔符（按优先级自动检测）：**制表符** → 连续 4+ 空格 → **逗号** → 连续 2+ 空格。
- 至少包含表头 + 一行数据。

### 3.2 表头识别规则

满足以下任一条件即识别为**节点表**（而非连线表）：

- 表头同时含列名 `id` 与 `label`（大小写不敏感）；或
- 表头含 `id`，且含 `au` / `ti` / `freq` / `title` / `author` 之一（VOSviewer / CiteSpace 文献表常见写法）。

### 3.3 推荐列名与映射

网站对常见列名有默认映射（表头**大小写不敏感**）：

| 导出列名 | 映射为 | 显示/变量名 |
|----------|--------|-------------|
| `id` | 节点 id | `id` |
| `label` | 主标签 | `label` |
| `au` | 主标签 | `au` |
| `py` | 副标签 | `year` |
| `ti` | 详情文本 | `Title` |
| `so` | 详情文本 | `source` |
| `freq` | 数值变量 | `Occurrences` |
| `bc` | 数值变量 | `Betweenness Centrality` |
| `x`, `y` | 数值变量 | `x`, `y` |

未在默认规则中的列：纯数字列视为 **Variable**，含非数字字符的列视为 **text**。列名 `id` 始终作为合并键。

### 3.4 示例（制表符分隔）

```
id	label	x	y	cluster
1	activity	0.5668	-0.5769	2
2	alternative	-0.1511	0.6776	3
```

VOSviewer / CiteSpace 文献节点表示例：

```
id	freq	bc	x	y	au	py	ti	so
0	1	0.02	116.25	550.19	*TIMEBANKS US	2010	...	TIMEBANKS DIR
1	1	0	151.29	757.08	Gilbert E	2005	...	ECON SOC
```

---

## 4. 连线表

### 4.1 文件结构

- 每行一条边，**2 或 3 列**；多列表格行不会被当作边。
- 空白分隔：`source target [weight]`
- 逗号分隔：`source, target, [weight]`
- `weight` 省略时默认为 `1`。
- 以 `*` 开头的行视为注释，跳过。

### 4.2 识别规则

- 非 Pajek 格式；
- 前若干行中多数匹配 `整数 整数` 或 `整数 整数 数值` 模式。

### 4.3 约束

- `source`、`target` 必须存在于已导入节点的 `id` 集合中，否则该边丢弃。
- 需**先**提供节点表，再提供连线表（或二者在同一合并文件中）。

### 4.4 示例

```
1 2 1.0
2 3 0.5
783, 385, 1.0
```

---

## 5. VOSviewer 合并导出（节点 + 连线单文件）

在节点表与连线表之间插入**独立一行**标记：

```
###Edges
```

或带边数（边数仅作标注，解析不依赖该数字）：

```
###Edges	2090
```

**结构**

```
<节点表头>
<节点数据行…>
###Edges
<source>, <target>, <weight>
…
```

**节点段**要求同 §3；**边段**要求同 §4。整文件识别为「节点+连线一体」，无需再单独导出连线文件。

**示例片段**

```
id	freq	bc	x	y	au	py	ti	so
790	6	0	128.16	221.65	Collom E	2011	null	SOCIOL FORUM
###Edges	2090
783, 385, 1.0
325, 759, 1.0
```

---

## 6. Pajek 格式

### 6.1 `.net`（网络）

**顶点段**

```
*Vertices <N>
<id> "<label>" <x> <y>
```

- `<N>` 为顶点数；后续每行一个顶点。
- `<id>` 为正整数，从 1 起连续编号时，可与 `.clu` / `.vec` 按序号对齐。
- 标签可含空格，须用双引号包裹。
- 坐标为浮点数。

**边段**（可选）

```
*Edges
<source> <target> [<weight>]
```

空白分隔；`weight` 可省略。

**示例**

```
*Vertices 3
1 "node A" 0.1 -0.2
2 "node B" -0.5 0.3
3 "node C" 0.0 0.0
*Edges
1 2 1
2 3 2
```

含 `*Vertices` 的 `.net` 默认作为**主网络**导入（节点 + 可选边）。

### 6.2 `.clu`（聚类分区）

- **扩展名必须为 `.clu`**（内容相同但扩展名为 `.txt` 不会被识别）。
- 结构：

```
*Vertices <N>
<cluster_id_1>
<cluster_id_2>
…
```

- 第 `i` 行标量对应 Pajek 顶点序号 `i`（1-based），网站生成 `id = "1"…"N"` 与主网络合并。
- 导入前须已有节点；按 `id` 写入 `cluster`。

### 6.3 `.vec`（向量 / 标量属性）

- **扩展名必须为 `.vec`**。
- 结构与 `.clu` 相同，每行为一个数值，映射为变量 `occurrences`（可后续在界面中改名）。

---

## 7. CiteSpace `.viz.json`（补充数据）

### 7.1 定位（必读）

| 能做 | 不能做 |
|------|--------|
| 作为**补充**，按 `id` 合并到已有节点（DOI、cid、簇名等） | **单独**拖入 JSON **不能**建成完整网络图 |
| 顶层含非空 `nodes[]` 的 CiteSpace 可视化导出 | 任意自定义 JSON / 其他软件的「项目文件」 |
| 使用 `nodes[].id` 与主表对齐 | 从 `sparse-matrix` **建边**（该字段当前**忽略**） |

典型工作流：**先**导出 Pajek `.net` 或节点表作为主网络，**再**导出 `.viz.json` 补充属性。

### 7.2 最低要求

```json
{
  "nodes": [
    { "id": "0", "label": "…", "x": 0, "y": 0, "cid": 1 }
  ]
}
```

- 扩展名：`.json`
- 顶层 **`nodes` 必须为非空数组**，否则拒绝（报错：`JSON 需含 nodes 数组`）。
- 每个节点对象至少提供 **`id`**（字符串或数字均可，导入时转字符串）。
- 编码：UTF-8。

### 7.3 常用字段

| 字段 | 用途 |
|------|------|
| `id` | 合并键（必填） |
| `label`, `key` | 标签相关（默认不勾选进画布，可在导入后勾选） |
| `x`, `y` | 坐标（默认不勾选；主网络已有坐标时通常不必再合并） |
| `cid` | 聚类编号 → `cluster`（默认勾选） |
| `size`, `degree`, `centrality` | 数值变量（默认不勾选） |
| `article` | 嵌套对象；其内**标量**字段展平为表列（如 `DOI` → `doi`，默认勾选） |
| `clusters[]` | 可选；`{ "cid": 1, "label": "簇名" }` 用于填充 `clusterName` |
| `sparse-matrix` | **不读取**；连线请用 `.net` / 边表 / `###Edges` 合并文件提供 |

**默认合并列**：`id`、`doi`、`cid`（→ `cluster`）。其余列导入后可在界面勾选。

### 7.4 与主节点的关系

- 主网络的 `id` 与 `nodes[].id` **必须同一套**（CiteSpace 导出时通常一致）。
- 无匹配 `id` 的 JSON 行会被忽略；匹配的行写入变量 / 聚类。
- 若尚未导入任何节点，JSON 仍可载入槽位，但**不会单独渲染网络**，需再导入主节点并应用。

---

## 8. 补充表（通用）

任意满足 §3 表头规则的表格，在已有节点的前提下，可作为补充导入：

```
id	doi	cid	clusterName
0	10.1234/example	1	Cluster A
1		2	Cluster B
```

| 列角色 | 表头建议 | 写入节点 |
|--------|----------|----------|
| 合并键 | `id` | — |
| 聚类编号 | `cid` | `cluster` |
| 聚类名称 | `clusterName` | `clusterName` |
| 数值 | 任意数字列 | Variable（列名 → 变量名） |
| 文本 | 任意文本列 | 详情 text 块 |

**特殊写入规则**

- 变量名 `occurrences` 或 `freq` → 同时更新节点 **weight**（大小）。
- 多份补充文件按导入顺序叠加；**同名 Variable 以后导入为准**。

---

## 9. 互操作：补充预设包（可选）

本网站可导出、也可重新导入如下文本包，适合在贵方工具中生成「已约定列映射」的补充数据：

```
# bibliometrics-supplement-v1
{"version":1,"columns":[…]}
---
id	doi	cid	clusterName
0	10.1234/example	1	Cluster A
```

- 第 1 行：固定标记 `# bibliometrics-supplement-v1`
- 第 2 行：JSON，`version` 为 `1`，`columns` 为列映射数组
- 第 3 行：`---`
- 第 4 行起：TSV 数据（表头 + 行）

若贵方工具不支持此格式，使用 §8 普通补充表即可。

---

## 10. 从常见软件迁出时的对照

| 来源 | 建议导出 | 本网站角色 |
|------|----------|------------|
| **CiteSpace** | Pajek `.net`（含 *Vertices* / *Edges*） | 主网络 |
| **CiteSpace** | `.viz.json` | 补充（DOI、cid 等） |
| **VOSviewer** | 合并网络 `.txt`（含 `###Edges`） | 节点+连线一体 |
| **VOSviewer** | 仅节点 `.txt` + 仅边 `.txt` | 拆成两个文件 |
| **Pajek** | `.net` | 主网络 |
| **Pajek** | `.clu` / `.vec` | 补充聚类 / 标量 |
| **通用** | `id` + `label` + `x`/`y` 表 | 节点表 |
| **通用** | `source` `target` `weight` 边表 | 连线表 |

---

## 11. 导出检查清单

在贵方软件中发布「导出到 Bibliometrics」功能前，建议确认：

1. **编码**：UTF-8，无 BOM 亦可。
2. **节点 `id`**：全文件唯一；连线端点、补充表使用同一套 `id`。
3. **主节点**：至少一个文件提供可识别的节点表或 Pajek `*Vertices`。
4. **连线**：若有边，提供合并文件（§5）或独立边表（§4），且端点 `id` 已存在于节点表。
5. **Pajek 标量**：`.clu` / `.vec` 顶点行数与 `*Vertices N` 一致，且与主网络顶点序号一致。
6. **列名**：尽量使用 §3.3 推荐名，减少用户手动映射。
7. **避免**：Excel `.xlsx`、PDF、图片；以及 **仅含 `sparse-matrix`、无 `nodes[]` 的 JSON**，或指望用 JSON 单独建网 / 从稀疏矩阵建边（当前均不支持，见 §7）。
8. **JSON**：若导出 `.viz.json`，须同时提供主网络文件（如 Pajek `.net`），且两边 `id` 一致。
---

## 12. 最小可工作示例

**方案 A — 单文件 Pajek**

`network.net`：§6.1 完整示例即可显示节点与边。

**方案 B — 节点表 + 聚类**

`nodes.txt`（§3.4） + `clusters.clu`（§6.2，顶点序与 `id` 1…N 对应）。

**方案 C — VOSviewer 一体导出**

`map.txt`：§5 合并格式，含 `id`/`au`/`x`/`y` 与 `###Edges` 段。

**方案 D — CiteSpace 双文件**

`共被引.net`（Pajek）+ `共被引.viz.json`（`nodes[].id` 与 `.net` 顶点 id 一致）。

---

## 修订记录

- 2026-06-17 — 初版（内部实现文档）
- 2026-06-17 — 改写为面向外部软件开发者的迁出格式规范
- 2026-07-30 — 强化 §7：JSON 仅为补充、不能单独成图、不读 sparse-matrix
